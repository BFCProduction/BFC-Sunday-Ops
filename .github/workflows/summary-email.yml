name: Sunday Summary Email (Retired)
on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Summary email retired
        run: echo "Sunday Ops summary email is retired. Use Settings > Reporting to export reports."
